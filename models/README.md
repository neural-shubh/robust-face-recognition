# Models

This folder contains trained model weights.

Due to GitHub's file size limits, pretrained models are not included.

Download the required models and place them in this directory before running the project and require details regarding models are already provided in readme
